{ 
  //enable GitDoc extension
    "gitdoc.enabled": true,
   // Commit delay: 10 minutes (600,000 ms)
    "gitdoc.autoCommitDelay": 600000,
    // Push delay: 10 minutes (600,000 ms)
    "gitdoc.autoPushDelay":60000,
    // Push automatically after commit
    "gitdoc.autoPush": "afterDelay",
  
    // Default commit message
    "gitdoc.commitMessageFormat": "Auto commit from GitDoc",
    // Optional: commit even if warnings exist
    "gitdoc.commitValidationLevel": "warning",
    
  }
  